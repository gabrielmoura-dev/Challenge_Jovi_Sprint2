# Verification evidence

Run `npm test` from the repository root. The browser suite covers the contained gallery dialog, album validation/persistence/focus return, functional gallery navigation, canonical camera ordering and camera routing, narrow viewport containment, and page errors.

The suite writes the reviewed captures here: `gallery-sheet.png`, `camera-12.png`, `camera-13.png`, `camera-sport.png`, and `camera-study.png`.
